<%= turbo_frame_tag "main-content" do %>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card shadow border-0 mb-4">
          <div class="card-header bg-transparent">
            <h1 class="text-center mb-0"><%= @unit.description %></h1>
          </div>

          <div class="card-body">
            <div class="row">
              <div class="col-md-12 mb-3">
                <label class="form-label"><strong>Detalles:</strong></label>
                <div class="mb-2">
                  <strong>Descripción:</strong> <%= @unit.description %>
                </div>
              </div>
            </div>

            <div class="text-center mt-4">
              <%= link_to 'Editar', edit_unit_path(@unit), class: 'btn btn-green me-2' %>
              <%= link_to 'Volver', units_path, class: 'btn btn-secondary' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
